# Copy this file to .env and fill in your values
# cp .env.example .env

# Database Configuration
POSTGRES_DB=loop-it
POSTGRES_USER=loop_user
POSTGRES_PASSWORD=your-secure-database-password-here

# JWT Configuration (CRITICAL: Use strong, unique secrets in production!)
JWT_SECRET=your-super-secure-jwt-secret-minimum-32-characters-long
JWT_REFRESH_SECRET=your-super-secure-refresh-secret-minimum-32-characters-long
JWT_EXPIRES_IN=7d

# Application Configuration
NODE_ENV=development
FRONTEND_URL=http://localhost:8080
VITE_API_URL=http://localhost:3000
VITE_APP_NAME=Loop-It

# Production Notes:
# - JWT_SECRET should be a cryptographically secure random string (64+ chars)
# - JWT_REFRESH_SECRET should be different from JWT_SECRET
# - POSTGRES_PASSWORD should be a strong, unique password
# - Never commit the actual .env file to version control!